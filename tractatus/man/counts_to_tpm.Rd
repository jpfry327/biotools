% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_transformation.R
\name{counts_to_tpm}
\alias{counts_to_tpm}
\title{Convert read counts to TPM (Transcripts Per Million)}
\usage{
counts_to_tpm(data, lengths, samples)
}
\arguments{
\item{data}{A data frame containing gene information and count data}

\item{lengths}{Column name containing gene lengths (unquoted)}

\item{samples}{Column selection for count columns (supports tidyselect)}
}
\value{
A data frame with count columns replaced by TPM values
}
\description{
This function converts raw read counts to TPM values, normalizing for both
sequencing depth and gene length. TPM is calculated as:
\enumerate{
\item Divide counts by gene length (in kb) to get RPK (Reads Per Kilobase)
\item Divide RPK by the sum of all RPK values and multiply by 1 million
}
}
\examples{
\dontrun{
# Convert counts to TPM for multiple samples
tpm_data <- counts_to_tpm(my_data, gene_length, starts_with("sample_"))

# Or specify exact columns
tpm_data <- counts_to_tpm(my_data, length, c(sample1, sample2, sample3))
}
}
